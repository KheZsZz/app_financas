import { StyleSheet } from "react-native";

export const Styles = StyleSheet.create ({
    container:{},
    
    input:{
        padding: 12,
        borderRadius: 8,
        borderWidth: 1,
        borderColor: '#ccc',
    }, 

    errorMensage:{
        color: 'red', 
        marginTop: 4 
    }
})