import { StyleSheet } from "react-native";

export const StylesMain = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center",
    alignContent: "center",
    padding: 8,
  },
  title: {
    fontSize: 22,
    fontWeight: "bold",
  },
});
